通过 Robo HardwareRuntime 执行一个设备动作。设备必须已连接、持有当前有效租约并显式武装，且动作必须由 Manifest 声明。`completed` 只表示设备完成了这条控制命令，返回的 `task_outcome` 固定为 `not_verified`；不得据此宣称用户任务完成，必须重新观察物理世界并调用 `robo_verify`。执行失败或设备状态未知时不要自动重放动作，应先观察状态；此工具的取消不等同于设备已停止。
