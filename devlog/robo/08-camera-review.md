# 真实相机增量审查

基点：`ea25608c48f50c208e1d9178da5fcc224b4842a1`。范围为本轮暂存的相机驱动、适配器、宿主接线、示例、测试和开发日志；不包含用户已有的 README/设计文档修改。

## Standards

独立审查发现首次授权等待冲突：Python 首帧等 8 秒，而 Swift 等待系统授权可达 30 秒。已统一为 Python 等待 35 秒。采集子进程另设独立进程组，由宿主处理 Ctrl+C 后再主动停止相机；父输入管道 EOF 仍负责宿主异常死亡时退出。审查者未复现此前推测的 Ctrl+C 状态错误，不把该推测登记为缺陷。

审查结论：1 项明确问题已修复，无剩余阻塞项，无需追加的设计气味建议。针对性协议测试独立运行 8 项通过（后续新增时钟回归另行验证）。

## Spec

1. 旧逻辑把超过 10 秒的源时间戳延迟改为“接收时间估计”，仍可能认为帧新鲜。审查通过协议替身复现后，已移除该降级：过期时间保持过期，读取层拒绝。无法确认时钟映射的观察可供查看，但不能授权提交，采集验证为 unknown。
2. 首次授权窗口 8/30 秒冲突同 Standards，已修复。

审查确认真实相机证据、观察权限、agent/程序共用通路、MHS 边界和物理验证 unknown 均清楚，无范围外行为。修复后独立复核相关 2 项测试通过，原报告无残余问题。

## 最终验证

- 相机协议回归：9 项通过；全量回归：217 项通过。
- 最终源码哈希对应的真实 FaceTime 采样、两版执行、版本隔离、重连、取消、宿主强杀与正常退出再次验证通过；结束时相机释放。
- 最终 wheel 重新安装后确认随包 Swift 源码 SHA-256 与工作区相同，并独立编译、发现真实设备。
- 实际图片保留在本机私有运行目录。仓库证据只包含测试结果、元数据和像素统计，不包含真实画面。
